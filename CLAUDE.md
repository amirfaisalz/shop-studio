# Development Guidelines

@AGENTS.md

## Critical Package Manager Rule

- **Always use Bun** (`bun install`, `bun run`, `bun add`, `bun test`, `bun dev`) as the package manager and JavaScript runtime for all operations in this project.
- Do NOT use `npm`, `yarn`, or `pnpm`.
